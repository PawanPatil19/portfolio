<aside class="singapore-signal" data-singapore-signal aria-label="Live Singapore time">
  <div class="signal-meta">
    <span>Singapore · live atmosphere</span>
    <span class="signal-actions">
      <time data-signal-time>SGT</time>
      <button
        class="observatory-toggle"
        type="button"
        data-observatory-open
        aria-controls="night-observatory"
        aria-expanded="false"
        aria-label="Open night observatory"
      >
        <span class="telescope-icon" aria-hidden="true">
          <i class="telescope-tube"></i>
          <i class="telescope-mount"></i>
          <i class="telescope-legs"></i>
        </span>
        <span>observe</span>
      </button>
    </span>
  </div>
  <p class="signal-caption" data-signal-caption>local time, rendered as atmosphere</p>
</aside>

<style>
  .singapore-signal {
    margin-top: 3.5rem;
    padding-top: 0.8rem;
    border-top: 1px solid color-mix(in srgb, var(--line), transparent 25%);
  }

  .signal-meta {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: var(--faint);
    font-size: 0.62rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .signal-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .observatory-toggle {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0;
    border: 0;
    background: transparent;
    color: var(--link);
    font: inherit;
    font-size: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    cursor: pointer;
    opacity: 0;
    visibility: hidden;
    transition: opacity 1.2s ease, color 180ms ease;
  }

  :global(html[data-sky='night']) .observatory-toggle {
    opacity: 1;
    visibility: visible;
  }

  .observatory-toggle:hover,
  .observatory-toggle:focus-visible { color: var(--text); }

  .observatory-toggle:focus-visible {
    outline: 1px solid var(--link);
    outline-offset: 0.35rem;
  }

  .telescope-icon {
    position: relative;
    display: inline-block;
    width: 1.35rem;
    height: 1rem;
  }

  .telescope-icon i {
    position: absolute;
    display: block;
  }

  .telescope-tube {
    top: 0.08rem;
    left: 0.1rem;
    width: 1rem;
    height: 0.3rem;
    border: 1px solid currentColor;
    transform: rotate(-18deg);
  }

  .telescope-tube::before,
  .telescope-tube::after {
    position: absolute;
    top: -0.12rem;
    width: 0.12rem;
    height: 0.46rem;
    background: currentColor;
    content: '';
  }

  .telescope-tube::before { left: -0.1rem; }
  .telescope-tube::after { right: -0.1rem; }

  .telescope-mount {
    top: 0.42rem;
    left: 0.65rem;
    width: 1px;
    height: 0.22rem;
    background: currentColor;
  }

  .telescope-legs {
    top: 0.6rem;
    left: 0.63rem;
    width: 1px;
    height: 0.38rem;
    background: currentColor;
    transform: rotate(20deg);
    transform-origin: top;
  }

  .telescope-legs::after {
    position: absolute;
    top: 0;
    left: 0;
    width: 1px;
    height: 0.38rem;
    background: currentColor;
    content: '';
    transform: rotate(-40deg);
    transform-origin: top;
  }

  .signal-caption {
    margin: 0.2rem 0 0;
    color: var(--faint);
    font-size: 0.64rem;
  }

  @media (max-width: 440px) {
    .signal-actions { gap: 0.65rem; }
    .observatory-toggle > span:last-child { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .observatory-toggle { transition: none; }
  }
</style>
