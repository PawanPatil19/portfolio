<button
  class="observatory-toggle"
  type="button"
  data-observatory-open
  aria-controls="night-observatory"
  aria-expanded="false"
  aria-label="Open night observatory"
  title="Open night observatory"
>
  <span class="telescope-icon" aria-hidden="true">
    <i class="telescope-tube"></i>
    <i class="telescope-mount"></i>
    <i class="telescope-legs"></i>
  </span>
</button>

<style>
  .observatory-toggle {
    position: fixed;
    right: 1.35rem;
    bottom: 1.35rem;
    z-index: 8;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.7rem;
    height: 2.7rem;
    padding: 0;
    border: 1px solid rgba(159, 181, 219, 0.24);
    border-radius: 50%;
    background: rgba(16, 17, 15, 0.52);
    color: var(--link);
    cursor: pointer;
    opacity: 0;
    visibility: hidden;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    transition: opacity 1.2s ease, color 180ms ease, border-color 180ms ease, transform 180ms ease;
  }

  :global(html[data-sky='night']) .observatory-toggle {
    opacity: 1;
    visibility: visible;
  }

  .observatory-toggle:hover,
  .observatory-toggle:focus-visible {
    border-color: rgba(159, 181, 219, 0.58);
    color: var(--text);
    transform: translateY(-2px);
  }

  .observatory-toggle:focus-visible {
    outline: 1px solid var(--link);
    outline-offset: 0.35rem;
  }

  .telescope-icon {
    position: relative;
    display: inline-block;
    width: 1.35rem;
    height: 1rem;
  }

  .telescope-icon i {
    position: absolute;
    display: block;
  }

  .telescope-tube {
    top: 0.08rem;
    left: 0.1rem;
    width: 1rem;
    height: 0.3rem;
    border: 1px solid currentColor;
    transform: rotate(-18deg);
  }

  .telescope-tube::before,
  .telescope-tube::after {
    position: absolute;
    top: -0.12rem;
    width: 0.12rem;
    height: 0.46rem;
    background: currentColor;
    content: '';
  }

  .telescope-tube::before { left: -0.1rem; }
  .telescope-tube::after { right: -0.1rem; }

  .telescope-mount {
    top: 0.42rem;
    left: 0.65rem;
    width: 1px;
    height: 0.22rem;
    background: currentColor;
  }

  .telescope-legs {
    top: 0.6rem;
    left: 0.63rem;
    width: 1px;
    height: 0.38rem;
    background: currentColor;
    transform: rotate(20deg);
    transform-origin: top;
  }

  .telescope-legs::after {
    position: absolute;
    top: 0;
    left: 0;
    width: 1px;
    height: 0.38rem;
    background: currentColor;
    content: '';
    transform: rotate(-40deg);
    transform-origin: top;
  }

  @media (prefers-reduced-motion: reduce) {
    .observatory-toggle { transition: none; }
  }
</style>
