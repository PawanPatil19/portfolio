<aside class="singapore-signal" data-singapore-signal aria-label="Live Singapore time">
  <div class="signal-meta">
    <span>Singapore · live atmosphere</span>
    <time data-signal-time>SGT</time>
  </div>
  <p class="signal-caption" data-signal-caption>local time, rendered as atmosphere</p>
</aside>

<style>
  .singapore-signal {
    margin-top: 3.5rem;
    padding-top: 0.8rem;
    border-top: 1px solid color-mix(in srgb, var(--line), transparent 25%);
  }

  .signal-meta {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: var(--faint);
    font-size: 0.62rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .signal-caption {
    margin: 0.2rem 0 0;
    color: var(--faint);
    font-size: 0.64rem;
  }
</style>
